package tui

// Version is written by the build script (build/main.go) or ldflags in CI.
// If this still says "UNSET", the binary was built incorrectly — use
// "go run ./build" instead of "go build".
var Version = "UNSET"
